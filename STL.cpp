#include<vector>
#include<set>
#include<string>
#include<map>
#include<queue>
#include <stack>
#include<pair>
#include <algorithm>

using namespace std

vector
{
    vector<int> num;
    vector<vector<int>> num1;
    vector<int> a[6];

    vector<int>::iterator it=num.begin();
    num.push_back();
    num.pop_back();
    num.size();
    num.clear();
    num.insert(it + 2, 2);
    num.erase(it + 2);
    num.erase(it + 2, it + 4);
}

set
{
    
}